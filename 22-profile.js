const firstName = 'firstName';
const lastName = 'lastName';
const year = 2017;
const multiply = (x, y) => {
    return x * y;
};

export {  // 优先写法
    firstName,
    lastName,
    year,
    multiply, //  函数名，调用的时候相应传参数
}